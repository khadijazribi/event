{% extends 'back.html.twig' %}

{% block title %}Reservation{% endblock %}

{% block body %}
    <h1>Reservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation.id }}</td>
            </tr>
            <tr>
                <th>id de l'evenement</th>
                <td>{{ reservation.event.id }}</td>
            </tr>
            <tr>
                <th>Id du user</th>
                <td>{{ reservation.user.id }}</td>
            </tr>
            <tr>
                <th>Transport</th>
                <td>{{ reservation.transport ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Nourriture</th>
                <td>{{ reservation.nourriture ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reservation_admin_index') }}" class="btn btn-primary">back to list</a>

    <a href="{{ path('reservation_admin_edit', {'id': reservation.id}) }}" class="btn btn-primary">edit</a>

    {{ include('reservation/_delete_form.html.twig') }}
{% endblock %}
