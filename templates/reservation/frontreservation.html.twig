{% extends 'base.html.twig' %}


{% block body %}
    <h1>Votre Réservation</h1>

    <table class="table">
        <tbody>
         <tr>
                <th>votre nom</th>
                <td>{{ reservation.user.nom }}</td>
            </tr>
            <tr>
                <th>nom de l'evénement</th>
                <td>{{ reservation.event.nom }}</td>
            </tr>
            <tr>
                <th>Transport</th>
                <td>{{ reservation.transport ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Nourriture</th>
                <td>{{ reservation.nourriture ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    

    <a class="btn btn-success" href="{{ path('reservation_edit', {'id': reservation.id}) }}">edit</a>

    {{ include('reservation/_delete_form.html.twig') }}
{% endblock %}
